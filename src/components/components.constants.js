const titleModalErro = 'Erro!';
const descricaoModalErroRedefinirSenha =
    'Não foi possível enviar um email de redefinição de senha para o endereço informado.';
const descricaoModalSucessoRedefinirSenha =
    'Um email de redefinição de senha foi enviado para o seu endereço de email.';
const titleModalSucesso = 'Sucesso!';

const texts = {
    titleModalErro,
    descricaoModalErroRedefinirSenha,
    descricaoModalSucessoRedefinirSenha,
    titleModalSucesso,
};

const constants = {
    texts,
};
export default constants;
